# phpstorm project files
.idea

# netbeans project files
nbproject

# zend studio for eclipse project files
.buildpath
.project
.settings

# windows thumbnail cache
Thumbs.db

# composer vendor dir
/vendor

# composer itself is not needed
composer.phar

# Mac DS_Store Files
.DS_Store

# phpunit itself is not needed
phpunit.phar
# local phpunit config
/phpunit.xml

tests/_output/*
tests/_support/_generated

#vagrant folder
/.vagrant.claude/

# File di configurazione specifici per ambiente (non sovrascrivere in deploy)
/config/db.php
mcp_server.log

# PDF F24/IMU generati: contengono dati fiscali reali dei contribuenti
/web/imu/*.pdf

# File caricati dagli utenti (avatar, catasto DB): dati runtime, non codice
/web/uploads/avatars/*
!/web/uploads/avatars/.gitkeep
/web/mappe/b542/uploads/*
!/web/mappe/b542/uploads/.gitkeep
