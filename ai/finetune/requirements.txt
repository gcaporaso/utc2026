# QLoRA fine-tuning UTC-BIM
# Installare con: pip install -r requirements.txt
# NOTA: richiede CUDA 11.8+ e una GPU NVIDIA

# Unsloth (include unsloth-zoo, ottimizzazioni custom)
unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git

# Training
trl>=0.7.4
transformers>=4.38.0
accelerate>=0.26.0
peft>=0.9.0
bitsandbytes>=0.42.0
datasets>=2.16.0

# Utilità
torch>=2.1.0
sentencepiece
protobuf
